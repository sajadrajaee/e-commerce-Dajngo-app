from django.apps import AppConfig


class UseraccountsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'useraccounts'
    verbose_name = "اعضای کانال"
    verbose_name_plural = "اعضای کانال"